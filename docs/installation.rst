============
Installation
============

At the command line::

    $ easy_install Django-Private-Beta

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv Django-Private-Beta
    $ pip install Django-Private-Beta
